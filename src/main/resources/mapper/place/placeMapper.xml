<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ucamp.gyeongjuma_be.place.repository.PlaceRepository">

    <insert id="saveAll">
        INSERT INTO place (
            place_id,
            place_name,
            add1,
            add2,
            tel,
            content_type_id,
            map_x,
            map_y,
            first_image,
            lcls_systm1,
            lcls_systm2,
            lcls_systm3,
            radius_meters,
            created_at,
            updated_at,
            is_active
        )
        VALUES
        <foreach collection="places" item="place" separator=",">
            (
                #{place.placeId},
                #{place.placeName},
                #{place.add1},
                #{place.add2},
                #{place.tel},
                #{place.contentTypeId},
                #{place.mapX},
                #{place.mapY},
                #{place.firstImage},
                #{place.lclsSystm1},
                #{place.lclsSystm2},
                #{place.lclsSystm3},
                #{place.radiusMeters},
                #{place.createdAt},
                #{place.updatedAt},
                #{place.isActive}
            )
        </foreach>
        ON DUPLICATE KEY UPDATE
            place_name = VALUES(place_name),
            add1 = VALUES(add1),
            add2 = VALUES(add2),
            tel = VALUES(tel),
            content_type_id = VALUES(content_type_id),
            map_x = VALUES(map_x),
            map_y = VALUES(map_y),
            first_image = VALUES(first_image),
            lcls_systm1 = VALUES(lcls_systm1),
            lcls_systm2 = VALUES(lcls_systm2),
            lcls_systm3 = VALUES(lcls_systm3),
            radius_meters = VALUES(radius_meters),
            updated_at = VALUES(updated_at),
            is_active = VALUES(is_active)
    </insert>

    <select id="findAll" resultType="com.ucamp.gyeongjuma_be.place.domain.Place">
        SELECT
            place_id AS placeId,
            place_name AS placeName,
            add1,
            add2,
            tel,
            content_type_id AS contentTypeId,
            map_x AS mapX,
            map_y AS mapY,
            first_image AS firstImage,
            lcls_systm1 AS lclsSystm1,
            lcls_systm2 AS lclsSystm2,
            lcls_systm3 AS lclsSystm3,
            radius_meters AS radiusMeters,
            created_at AS createdAt,
            updated_at AS updatedAt,
            deleted_at AS deletedAt,
            is_active AS isActive
        FROM place
        WHERE is_active = TRUE
        ORDER BY place_id DESC
    </select>

    <select id="searchPlaces" resultType="com.ucamp.gyeongjuma_be.place.dto.PlaceSearchResponse">
        SELECT
            p.place_id AS place_id,
            p.place_name AS place_name,
            COALESCE(pc.description, '') AS description, <!-- 추후 구현 예정 -->
            COALESCE(p.lcls_systm3, p.lcls_systm2, p.lcls_systm1, '') AS category,
            0.0 AS rating, <!-- 추후 구현 예정 -->
            0 AS reviewCount, <!-- 추후 구현 예정 -->
            0 AS likes, <!-- 추후 구현 예정 -->
            p.map_y AS lat,
            p.map_x AS lng,
            <choose>
                <when test="latitude != null and longitude != null">
                    ROUND(
                        6371 * ACOS(
                            LEAST(1, GREATEST(-1,
                                COS(RADIANS(#{latitude}))
                                * COS(RADIANS(p.map_y))
                                * COS(RADIANS(p.map_x) - RADIANS(#{longitude}))
                                + SIN(RADIANS(#{latitude}))
                                * SIN(RADIANS(p.map_y))
                            ))
                        ),
                        2
                    )
                </when>
                <otherwise>
                    NULL
                </otherwise>
            </choose> AS distance,
            CASE
                WHEN cl.score IS NULL THEN '정보 없음'
                WHEN cl.score REGEXP '^[0-9]+(\\.[0-9]+)?$'
                 AND CAST(cl.score AS DECIMAL(5, 2)) BETWEEN 0 AND 30 THEN '여유'
                WHEN cl.score REGEXP '^[0-9]+(\\.[0-9]+)?$'
                 AND CAST(cl.score AS DECIMAL(5, 2)) BETWEEN 31 AND 60 THEN '보통'
                WHEN cl.score REGEXP '^[0-9]+(\\.[0-9]+)?$'
                 AND CAST(cl.score AS DECIMAL(5, 2)) BETWEEN 61 AND 100 THEN '혼잡'
                ELSE NULL
            END AS congestion,
            '정보 없음' AS operationStatus, <!-- 추후 구현 예정 -->
            CASE WHEN v.place_id IS NULL THEN FALSE ELSE TRUE END AS isVisited,
            p.first_image AS imageUrl
        FROM place p
        LEFT JOIN visit v
          ON v.place_id = p.place_id
         AND v.member_id = #{memberId}
        LEFT JOIN place_content pc
          ON p.place_id = pc.place_id
        LEFT JOIN congestion_log cl
          ON cl.place_id = p.place_id
         AND cl.log_date = CURDATE()
         AND cl.is_active = TRUE
        WHERE p.is_active = TRUE
        <if test="search != null and search != ''">
          AND p.place_name LIKE CONCAT('%', #{search}, '%')
        </if>
        ORDER BY
        <choose>
            <when test="sort == 'unvisited'">
                CASE WHEN v.place_id IS NULL THEN 0 ELSE 1 END ASC,
                p.place_name ASC
            </when>
            <when test="sort == 'congestion'">
                CASE
                    WHEN cl.score REGEXP '^[0-9]+(\\.[0-9]+)?$' THEN 0
                    ELSE 1
                END ASC,
                CAST(cl.score AS DECIMAL(5, 2)) DESC,
                p.place_name ASC
            </when>
            <otherwise>
                <choose>
                    <when test="latitude != null and longitude != null">
                        distance ASC,
                        p.place_name ASC
                    </when>
                    <otherwise>
                        p.place_name ASC
                    </otherwise>
                </choose>
            </otherwise>
        </choose>
    </select>

</mapper>
